apiVersion: v1
kind: Service
metadata:
  name: capstone-app-service
spec:
  type: NodePort
  selector:
    app: capstone-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30459